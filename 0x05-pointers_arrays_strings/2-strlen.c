#include "main.h"

/**
* _strlen - Reads the length of string 
*
* @*s - Returns the value of a string
*
*Return - l;
*/

int _strlen(char *s)
{
int length = 0;
int k;
	for (k = 0; ,k < length; k++)
	{
		length += s[k];
	}
	return (length);


#include <stdio.h>
#include <string.h>
#include "main.h"
/**
* rev_string - reverses a string
* @s: pointing to a string.
* Return: Always 0 (Success);
*/

void puts2(char *str)
{
int m, l;
  for (m = 0; m < l; m++)
  {
	  printf("%c", str[m]);
	m = m + 1;
  }
	printf("\n");
}
